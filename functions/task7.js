// Создайте функцию avg() , которая будет находить среднее значение 3х своих аргументов (аргументы величины числовые).
function avg(a, b, c) {
    return (a + b + c) / 3;
}

console.log(avg(2, 2, 5));
