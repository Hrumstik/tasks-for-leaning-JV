// Опишите функцию с именем min, которая вычисляет минимальное из двух чисел, переданных в неё.
function min(a, b) {
    if (a > b) {
        return b;
    }
    else return a
}

console.log(min(2, 1));